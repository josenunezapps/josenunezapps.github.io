const c=window.SITE_CONFIG;const set=(id,value)=>{const el=document.getElementById(id);if(el)el.textContent=value};set("brandName",c.businessName);set("footerName",c.businessName);set("businessType",c.businessType);set("heroTitle",c.heroTitle);set("heroText",c.heroText);set("hours",c.hours);set("address",c.address);set("promoHero",c.promoHero);set("promoPrice",c.promoPrice);set("promoSecondary",c.promoSecondary);set("promoSecondaryPrice",c.promoSecondaryPrice);const wa=`https://wa.me/${c.whatsapp}?text=${encodeURIComponent("Hola, quiero hacer una consulta.")}`;document.getElementById("waTop").href=wa;const grid=document.getElementById("productsGrid");grid.innerHTML=c.products.map(p=>`<article class="card"><h3>${p.name}</h3><p>${p.description}</p><div class="price">${p.price}</div></article>`).join("");document.getElementById("contactForm").addEventListener("submit",e=>{e.preventDefault();const name=document.getElementById("name").value.trim();const msg=document.getElementById("message").value.trim();const text=`Hola, soy ${name}. ${msg}`;document.getElementById("formStatus").textContent="Abriendo WhatsApp…";window.open(`https://wa.me/${c.whatsapp}?text=${encodeURIComponent(text)}`,"_blank")});